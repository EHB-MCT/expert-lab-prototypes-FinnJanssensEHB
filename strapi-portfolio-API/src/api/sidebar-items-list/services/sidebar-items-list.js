'use strict';

/**
 * sidebar-items-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sidebar-items-list.sidebar-items-list');
